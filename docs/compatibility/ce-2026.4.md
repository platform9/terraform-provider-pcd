# PCD Community Edition 2026.4 — compatibility evidence

Recorded from the live CE lab (`https://pcd.pf9.io`) during Step 0 preflight on
**2026-07-10**. This file is committed evidence that gates scope decisions marked
`[EXT]`/`[DISC]` in the implementation plan. Re-run these probes against each new PCD
version. Raw probe responses are not committed (they may contain lab-specific IDs).

## Access
- Endpoint `https://pcd.pf9.io`, self-signed TLS (`insecure = true`).
- Auth verified: password → project `service`, domain `default`, region `Infra`.
- Live token scope: user `admin@airctl.localnet`, project id
  `369e7e9f49704ad58e49010b67312696`, roles: `admin, manager, member, reader,
  load-balancer_member, load-balancer_observer`.

## Service catalog (14 service types)
`compute, dns, hamgr, identity, image, image-cluster, infra-optim, key-manager,
load-balancer, mors, network, placement, regioninfo, resmgr, volumev3`

Absent (confirms plan §8.13 "not applicable"): object-store/swift, orchestration/heat,
shared-filesystem/manila, container-infra/magnum, database/trove, workflow/mistral.

## API versions
- Nova max microversion: **2.100**
- Cinder: **v3.0**
- Glance v2 reachable (image list empty)

## Neutron extensions — 91 aliases (the `[EXT]` gate)

Notable for scope decisions:

| Capability | Alias(es) | Present | Disposition impact |
|---|---|---|---|
| QoS | `qos` (+ ~20 `qos-*`) | yes | QoS resources shippable (Phase 3) |
| Address groups | `address-group` | yes | `networking_address_group` shippable |
| Trunk ports | `trunk`, `trunk-details` | yes | `networking_trunk` shippable |
| FIP port forwarding | `floating-ip-port-forwarding-detail` | yes | `networking_portforwarding` shippable |
| RBAC policies | `rbac-policies` | yes | `networking_rbac_policy` shippable |
| Address scopes / subnet pools | `address-scope`, `subnet_allocation`, `subnetpool-prefix-ops` | yes | shippable |
| VPNaaS | `vpnaas`, `vpn-endpoint-groups`, `vpn-flavors` | yes | VPNaaS family shippable (plan guessed disabled) |
| L3 HA / flavors | `l3-ha`, `l3-flavors`, `flavors` | yes | — |
| **BGP dynamic routing** | `bgp`/`bgp-speaker` | **no** | `networking_bgp_*` → NA/conditional (plan had parity) |
| **Segments** | `segment`/`network-segment-range` | **no** | `networking_segment` → conditional (plan had parity) |
| **FWaaS v2** | `fwaas`/`fwaas_v2` | **no** | `fw_*` → NA (confirmed) |
| **BGPVPN** | `bgpvpn` | **no** | `bgpvpn_*` → NA (confirmed) |
| **TaaS** | tap-mirror | **no** | `taas_*` → NA (confirmed) |

## PCD-specific services
- **resmgr v1** `GET /resmgr/v1/hosts` → 200. Two hosts:
  - `afc7bdb1-d90f-44f5-8d31-8c03cea81444` (`pcd-host-01`): roles `pf9-glance-role`,
    `pf9-ip-discovery`, `pf9-neutron-base`, `pf9-neutron-ovn-controller`,
    `pf9-neutron-ovn-metadata-agent`, `pf9-ostackhost-neutron`; hypervisor_type kvm.
  - `0d7f321b-6d1a-4e05-bf74-14337d3db8af` (`pcd-ce-jul-hyp`): roles `[]`.
- **resmgr v2** `GET /resmgr/v2/blueprint` → 200. Existing blueprint (schema evidence for
  `pcd_cluster_blueprint`): `name="BP"`, `networkingType="ovn"`,
  `enableDistributedRouting=true`, `virtualNetworking={enabled, underlayType:"vlan",
  vnidRange:"50:60"}`, `imageLibraryStorage`, `vmStorage`, `storageBackends={}`.
  `GET /resmgr/v2/hostconfigs` → 200.
- **hamgr**: `GET /hamgr/` → 200 body `true`; `GET /hamgr/v1/` → 404. API shape `[DISC]`.
- **mors**: `GET /mors/` and `/mors/v1/` → 404 (service up; routes unknown). `[DISC]`.

## Liveness probes
| Service | Probe | Result |
|---|---|---|
| Barbican | `GET /barbican/v1/secrets` | 200 `{"secrets":[],"total":0}` — **live** |
| Octavia | `GET /octavia/v2/lbaas/loadbalancers` | 200 — **live** |
| Designate | `GET /designate/v2/zones` | 200 — **live** |

## Compute capacity (Gate A) — NOT currently satisfied
- `nova-scheduler` up, `nova-conductor` up.
- Only compute host `pcd-host-01`'s `nova-compute` is **down** → hypervisor `state=down`,
  `running_vms=0`. Glance images / Neutron networks empty. A VM cannot be scheduled until a
  hypervisor is brought up, a network is created, and an image is uploaded. Tiny flavors
  (`m1.tiny` 1/512, `m1.small` 1/2048) are available for capacity-disciplined tests.
