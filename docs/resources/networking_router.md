---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pcd_networking_router Resource - pcd"
subcategory: "Networking"
description: |-
  Manages a Neutron router in PCD.
---

# pcd_networking_router (Resource)

Manages a Neutron router in PCD.

## Example Usage

```terraform
resource "pcd_networking_network" "external" {
  name     = "tf-example-external-net"
  external = true
}

resource "pcd_networking_router" "example" {
  name                = "tf-example-router"
  description         = "Router managed by Terraform"
  admin_state_up      = true
  external_network_id = pcd_networking_network.external.id
  enable_snat         = true
  tags                = ["terraform", "example"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `admin_state_up` (Boolean) The administrative state of the router.
- `description` (String) A description of the router.
- `enable_snat` (Boolean) Whether SNAT is enabled on the gateway. Requires external_network_id.
- `external_network_id` (String) The ID of the external network for the router's gateway.
- `name` (String) The name of the router.
- `region` (String) The region. Defaults to the provider's region.
- `tags` (Set of String) Tags applied to the router.
- `tenant_id` (String) The owning project. Changing this forces a new resource.

### Read-Only

- `id` (String) The router ID.

## Import

Import is supported using the following syntax:

```shell
terraform import pcd_networking_router.example <id>
```
