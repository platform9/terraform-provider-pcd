---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pcd_identity_role_assignment Resource - pcd"
subcategory: ""
description: |-
  Assigns a role to a user or group on a project or domain. Assignments are immutable: any change forces a new resource. Exactly one of user_id/group_id and exactly one of project_id/domain_id must be set.
---

# pcd_identity_role_assignment (Resource)

Assigns a role to a user or group on a project or domain. Assignments are immutable: any change forces a new resource. Exactly one of `user_id`/`group_id` and exactly one of `project_id`/`domain_id` must be set.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `role_id` (String) The role to assign.

### Optional

- `domain_id` (String) The domain the assignment is scoped to (mutually exclusive with project_id).
- `group_id` (String) The group to assign the role to (mutually exclusive with user_id).
- `project_id` (String) The project the assignment is scoped to (mutually exclusive with domain_id).
- `region` (String) The region. Defaults to the provider's region.
- `user_id` (String) The user to assign the role to (mutually exclusive with group_id).

### Read-Only

- `id` (String) Composite ID: `domain_id/project_id/group_id/user_id/role_id`.
