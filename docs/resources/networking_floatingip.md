---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pcd_networking_floatingip Resource - PCD"
subcategory: "Networking"
description: |-
  Manages a floating IP allocated from an external network in PCD. Optionally associate it with a port to expose an instance externally.
---

# pcd_networking_floatingip (Resource)

Manages a floating IP allocated from an external network in PCD. Optionally associate it with a port to expose an instance externally.

## Example Usage

```terraform
resource "pcd_networking_network" "example" {
  name = "tf-example-network"
}

resource "pcd_networking_subnet" "example" {
  network_id = pcd_networking_network.example.id
  name       = "tf-example-subnet"
  cidr       = "10.0.0.0/24"
}

resource "pcd_networking_port" "example" {
  network_id = pcd_networking_network.example.id
  name       = "tf-example-port"

  depends_on = [pcd_networking_subnet.example]
}

resource "pcd_networking_floatingip" "example" {
  pool        = "external-net"
  description = "Floating IP managed by Terraform"
  port_id     = pcd_networking_port.example.id
  tags        = ["env:dev", "managed-by:terraform"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `pool` (String) The name of the external network to allocate the floating IP from. Changing this forces a new resource.

### Optional

- `address` (String) The floating IP address. Request a specific address by setting this; changing it forces a new resource.
- `description` (String) A description of the floating IP.
- `fixed_ip` (String) The specific fixed IP on the associated port to map to. Defaults to the port's first address.
- `port_id` (String) The port to associate the floating IP with. Set to a port ID to associate, or to an empty string to disassociate. Leave unset to manage the association with a separate `pcd_networking_floatingip_associate` resource.
- `region` (String) The region. Defaults to the provider's region.
- `tags` (Set of String) Tags applied to the floating IP.
- `tenant_id` (String) The owning project. Changing this forces a new resource.

### Read-Only

- `floating_network_id` (String) The ID of the external network `pool` resolved to.
- `id` (String) The floating IP ID.
- `router_id` (String) The router through which the floating IP is routed.
- `status` (String) The operational status of the floating IP.

## Import

Import is supported using the following syntax:

```shell
terraform import pcd_networking_floatingip.example <id>
```
