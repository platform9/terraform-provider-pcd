---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "pcd_networking_subnet_route Resource - PCD"
subcategory: "Networking"
description: |-
  Manages a single host route on a Neutron subnet (advertised to instances via DHCP) without disturbing routes managed elsewhere. Every attribute forces a new resource. Multiple subnet_route resources on the same subnet are applied serially so they do not clobber each other.
---

# pcd_networking_subnet_route (Resource)

Manages a single host route on a Neutron subnet (advertised to instances via DHCP) without disturbing routes managed elsewhere. Every attribute forces a new resource. Multiple subnet_route resources on the same subnet are applied serially so they do not clobber each other.

## Example Usage

```terraform
resource "pcd_networking_network" "example" {
  name = "tf-example-network"
}

resource "pcd_networking_subnet" "example" {
  network_id = pcd_networking_network.example.id
  name       = "tf-example-subnet"
  cidr       = "10.0.0.0/24"
}

resource "pcd_networking_subnet_route" "example" {
  subnet_id        = pcd_networking_subnet.example.id
  destination_cidr = "192.168.100.0/24"
  next_hop         = "10.0.0.1"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `destination_cidr` (String) The destination CIDR. Changing this forces a new resource.
- `next_hop` (String) The next-hop IP address. Changing this forces a new resource.
- `subnet_id` (String) The subnet to add the host route to. Changing this forces a new resource.

### Optional

- `region` (String) The region. Defaults to the provider's region.

### Read-Only

- `id` (String) Composite ID: `subnet_id/destination_cidr/next_hop`.

## Import

Import is supported using the following syntax:

```shell
terraform import pcd_networking_subnet_route.example <subnet_id>/<destination_cidr>/<next_hop>
```
