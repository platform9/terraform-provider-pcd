# Provider binary / release artifacts
terraform-provider-pcd
dist/

# Go
*.test
*.out
coverage.txt

# Terraform working files
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Credentials & secrets — NEVER commit
*.env
.env
clouds.yaml
*.pem
*.key
*.asc
*.gpg
secrets.*

# Local dev overrides
.terraformrc
terraform.rc
*.tfrc

# The CE deployment handoff must never enter the repo
PCD-CE-Deployment-Handoff.md

# Local, untracked working-dir docs (kept on the developer's machine, never committed):
# maintainer-internal notes and evidence that are not appropriate for the public repo.
CLAUDE.md
PCD-Terraform-Provider-Validation-Status.md
DECISIONS.md
docs/compatibility/ce-2026.4.md
# Agent-authored implementation plans and design specs. Working notes, not provider
# documentation: the registry never served them, and they carried about three quarters
# of the weight of docs/ into every release tarball.
docs/superpowers/

# Generated registry docs (docs/index.md, docs/resources/, docs/data-sources/, docs/guides/)
# ARE committed: the Terraform Registry renders provider documentation from the docs/ tree at
# the release tag, so it must be in the repo. Regenerate with `make generate` (tfplugindocs)
# and commit the result. The maintainer-internal notes listed above — docs/compatibility/ and
# docs/superpowers/ — are the only parts of docs/ that stay untracked.

# Editors / OS
.idea/
.vscode/
.DS_Store
